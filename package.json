{
  "name": "aryeo-sdk",
  "private": true,
  "description": "Typed clients for the Aryeo real estate media platform, generated from one verified description of the API.",
  "license": "MIT",
  "author": "Envesko",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/envesko/aryeo-sdk.git"
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node tools/validate-manifest.mjs",
    "generate": "node codegen/index.mjs",
    "check": "npm run validate && npm run generate -- --check",
    "test": "npm run validate && npm run scan && npm run check && npm run typecheck && npm run conformance",
    "scan": "node tools/scan-evidence.mjs",
    "typecheck": "tsc --noEmit -p packages/client-ts/tsconfig.json",
    "build": "tsc -p packages/client-ts/tsconfig.json",
    "conformance": "npm run build && node --test conformance/cases/client.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
